-- CreateTable
CREATE TABLE "helpQuery" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "email" TEXT NOT NULL,
    "query" TEXT NOT NULL,

    CONSTRAINT "helpQuery_pkey" PRIMARY KEY ("id")
);
